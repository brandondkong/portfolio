---
import type { SanityDocument } from "@sanity/client";
import { sanityClient } from "sanity:client";

import { urlFor } from "../../lib/sanity";

import Section from "../Section.astro";
import Text from "../Text.astro";

interface Experience extends SanityDocument {
  company: {
    name: string;
    logo: { asset: { _ref: string; _type: string } } | null;
  };
  role: string;
  startDate: string;
  endDate: string | null;
  description: string | null;
  order: number;
}

const EXPERIENCE_QUERY = `*[_type == "experience"]|order(order asc){ _id, company->{ name, logo }, role, startDate, endDate, description, order }`;
const experiences = await sanityClient.fetch<Experience[]>(EXPERIENCE_QUERY);

function formatDate(dateStr: string): string {
  const date = new Date(dateStr + "T00:00:00");
  return date.toLocaleDateString("en-US", { month: "short", year: "numeric" });
}

function formatRange(start: string, end: string | null): string {
  return `${formatDate(start)} — ${end ? formatDate(end) : "Present"}`;
}
---

<Section title="Experience" class="pt-section-top pb-section-bottom flex flex-col gap-section-gap">
  <div class="flex flex-col">
    {experiences.map((exp, i) => (
      <div class:list={["py-10 sm:py-14 flex flex-col gap-content-gap", i !== experiences.length - 1 && "border-b border-border"]}>
        <div class="flex items-center gap-tight-gap">
          {exp.company.logo && (
            <img
              src={urlFor(exp.company.logo).width(80).height(80).url()}
              alt={exp.company.name}
              width={32}
              height={32}
              class="rounded"
            />
          )}
          <Text variant="overline">{formatRange(exp.startDate, exp.endDate)}</Text>
        </div>
        <div class="flex flex-col lg:flex-row lg:items-start lg:justify-between gap-element-gap lg:gap-section-gap">
          <div class="flex flex-col gap-tight-gap">
            <Text variant="body-xl" as="h3" class="font-semibold italic">{exp.company.name}</Text>
            <Text variant="body" class="text-muted">{exp.role}</Text>
          </div>
          {exp.description && (
            <Text variant="body-lg" class="text-muted text-justify max-w-md lg:pb-1 lg:indent-20">{exp.description}</Text>
          )}
        </div>
      </div>
    ))}
  </div>
</Section>
