import { defineCliConfig } from 'sanity/cli';

export default defineCliConfig({
    api: {
        projectId: 'mpaopctd',
        dataset: 'production',
    },
    deployment: {
        appId: 'swrzl4ci6bdsl0bf5qspqali',
    }
});
